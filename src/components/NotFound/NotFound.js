import React from 'react';
import './NotFound.css'
const NotFound = () => (
  <div class="NotFound">
  <div class="Message"><p>Seems like there is some issue :( <br></br>Let's go <a href="/">home</a> and try from there.</p></div>
    </div>
)

export default NotFound;
